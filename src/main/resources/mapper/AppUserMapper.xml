<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.namedmoment.mapper.AppUserMapper">

    <select id="selectByUsername" resultType="com.example.namedmoment.entity.AppUser">
        SELECT id, username, password_hash, created_at
        FROM app_user
        WHERE username = #{username}
    </select>

    <select id="selectById" resultType="com.example.namedmoment.entity.AppUser">
        SELECT id, username, password_hash, created_at
        FROM app_user
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.namedmoment.entity.AppUser"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_user (username, password_hash, created_at)
        VALUES (#{username}, #{passwordHash}, #{createdAt})
    </insert>

</mapper>
